spring:
  application:
    name: demo-client
  profiles:
    active: test0
  cloud:
    config:
      uri: ${CONFIG_URL:http://localhost:8373}

---

spring:
  profiles: test0

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_URL:http://localhost:8370/eureka/}
    registerWithEureka: true


---

spring:
  profiles: test1

server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_URL:http://localhost:8370/eureka/}
    registerWithEureka: true