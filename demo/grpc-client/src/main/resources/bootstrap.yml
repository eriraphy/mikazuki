server:
  port: 6566

spring:
    application:
        name: grpc-client

eureka:
  client:
    register-with-eureka: true
    serviceUrl:
        defaultZone: ${DISCOVERY_URL:http://localhost:8370/eureka/}
